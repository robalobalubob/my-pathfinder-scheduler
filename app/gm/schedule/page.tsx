import GMScheduleClient from "../../../components/GMScheduleClient";

export const metadata = {
  title: "GM Scheduling Page",
};

export default function GMSchedulePage() {
  return <GMScheduleClient />;
}